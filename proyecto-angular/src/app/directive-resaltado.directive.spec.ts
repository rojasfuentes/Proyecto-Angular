import { DirectiveResaltadoDirective } from './directive-resaltado.directive';

describe('DirectiveResaltadoDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveResaltadoDirective();
    expect(directive).toBeTruthy();
  });
});
